You are a business analyst on the Helios Group delivery team. Your job is to write acceptance criteria for ticket HEL-142, "Show estimated delivery window in the cart", so that a developer can build it and a tester can check it.

Read these inputs. Do not paste them back to me.

1. helios-backlog/HEL-142.json - the ticket. Note that acceptance_criteria is empty; that is what we are fixing.
2. docs/specs/ordercore-fsd.txt - the OrderCore functional specification. The rules for this feature are in section 7, "Cart and delivery window", which has sub-sections 7.1 to 7.5. Read section 7 carefully. You may skim the rest of the document if you need context on terms, but the criteria come from section 7 and the ticket.
3. docs/design/HEL-142-hld.md - the draft high-level design. It is short and it is not approved. If it disagrees with the specification, the specification wins and you should flag the disagreement as an open question.

Some background so you understand why this matters. Customers phone the contact centre to ask when an order will arrive. The information exists but is never shown before checkout. Marketing want the window shown as a confident statement, not a hedged one. The warehouse team hold the stock location and carrier cut-off data in a spreadsheet, and nobody has decided where that data should live for this feature. That last point is important: your criteria should state the rule that must hold and should not decide where the data comes from.

What to produce, in a file called week2/ac-v900.md:

A heading with the ticket key and summary.

Then a numbered list of acceptance criteria in Given / When / Then form. Each criterion should be one scenario that a tester could set up and check. After each criterion, add one line saying which part of section 7 (or the ticket) it comes from, for example "Source: FSD 7.2, dispatch date rule". Cover, at minimum: the window is shown on the cart before the customer confirms; the exact display format including the same-month, cross-month and same-day cases; the carrier cut-off rule; the effect of destination region on transit; the behaviour when the window cannot be calculated; and the wording constraint that bans hedging words. Add anything else in section 7 that a tester would need to check.

Then a heading "Open questions" with a bulleted list of anything the ticket or the specification leaves undecided. Do not answer the questions or propose a solution. Just list them. I expect the data source question to be one of them.

For the display format, the specification gives three cases: earliest and latest dates in the same month ("Arrives 18-20 March"), in different months ("Arrives 30 April - 2 May"), and the same date ("Arrives 18 March"). Write one criterion for each of the three, using those exact strings as the expected result, because a tester will compare the screen against them character by character.

For the region rule, use the regions and transit days from Table 5 in section 7.2. Pick one mainland case and one non-mainland case; you do not need a criterion per region.

Rules: every Then must describe something a tester can observe. Do not include prose explanations, a summary, or a risks section. Do not invent rules that are not in the ticket or in section 7; if you think something is missing, put it under open questions. Use the terminology from the specification. Keep the file under 80 lines.

When you have written the file, reply in the chat with one line: the number of criteria and the number of open questions. Nothing else.
